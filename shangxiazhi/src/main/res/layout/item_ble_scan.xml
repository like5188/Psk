<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="bleScanInfo"
            type="com.psk.shangxiazhi.devices.BleScanInfo" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        android:background="@drawable/setting_bg_choose"
        android:clickable="true"
        android:focusable="true"
        android:orientation="horizontal">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="15dp"
            android:paddingTop="10dp"
            android:paddingRight="15dp"
            android:paddingBottom="10dp"
            android:text="@{bleScanInfo.name}"
            android:textColor="@drawable/setting_text_color_choose"
            android:textSize="24sp"
            android:textStyle="bold"
            tools:text="SJZG-TEST" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="end"
            android:paddingLeft="15dp"
            android:paddingTop="10dp"
            android:paddingRight="15dp"
            android:paddingBottom="10dp"
            android:text="@{bleScanInfo.address}"
            android:textColor="@drawable/setting_text_color_choose"
            android:textSize="24sp"
            android:textStyle="bold"
            tools:text="SJZG-TEST" />
    </LinearLayout>

</layout>






